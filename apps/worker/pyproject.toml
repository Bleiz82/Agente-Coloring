[project]
name = "colorforge-worker"
version = "0.1.0"
description = "ColorForge AI worker process — browser automation"
requires-python = ">=3.12"
dependencies = [
    "loguru>=0.7.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
